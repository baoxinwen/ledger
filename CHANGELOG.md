# Changelog

本项目的所有重要变更记录于此文件。
版本号遵循 [SemVer](https://semver.org/lang/zh-CN/) 语义化版本。

## [1.0.1] - 2026-09-04

数据正确性与安全性修复批次：集中解决一批"操作提示成功但实际未生效"、
账目被静默算错的正确性问题，并完成一轮依赖与安全加固。

### 亮点

- 记账数据更可信：修复清空备注不生效、预算改回"总预算"不生效、
  连续记账时日期被静默改回今天等问题
- 统计不再被脏数据污染：导入时拒绝"2 月 30 日"这类不存在的日期，
  收入分类不能再创建永远为零的无效预算
- 安全加固：依赖漏洞通告清零（npm audit 高危 0），设置接口不再把
  数据库内部错误细节泄露给浏览器
- 备份更省心：恢复前的自动快照按上限滚动清理，不再无限占满磁盘

### 破坏性变更

无。

### 修复

- 编辑记录清空备注后保存，旧备注仍保留（提示成功但实际未更新）
- 编辑预算改回"总预算"后保存，预算仍挂在原分类下
- "保存并再记"连续记账时，已选日期被静默重置为今天
- 导入账单接受"2026-02-30"这类日历上不存在的日期，记录永久不参与
  统计与预算，收支被静默算少
- 预算接口不再接受收入分类，杜绝执行金额恒为 0 的无效预算行
- 设置接口在数据库/文件系统故障时按 500 返回通用文案，不再透传
  内部错误原文
- 反复执行备份恢复时，恢复前快照按保留上限（最近 10 份）滚动清理
- 首页预算状态卡、导入历史分页在弱网下可能被过期响应覆盖
- 删除当前页最后一条记录后停留在空白越界页
- 快速记账中新建标签失败时静默丢失且无提示；统计页加载失败增加
  "重试"按钮；详情抽屉日期口径与列表一致
- Windows 工作树的 entrypoint 行尾损坏导致本地 `docker build` 出的
  镜像无法启动，新增守卫测试防止复发

### 测试与工程

- 修复两条从未真实生效的渐变回归 e2e（空集合恒真断言），补数量护栏
- CI 三个 job 增加显式超时
- 依赖升级：better-sqlite3 13、jest 30.5、morgan 1.12、jsdom 30、
  typescript-eslint 8.68、@testing-library/react 16.3.3，及 GitHub
  Actions（checkout v7、upload-artifact v7、login-action v4、
  build-push-action v7、setup-buildx v4）
- `npm audit` 高危漏洞清零（glob、qs 通告已在 semver 范围内升级修复）

## [1.0.0] - 2026-09-02

首个正式版本：本地优先、可自行部署的中文个人记账应用。

### 亮点

- 收支记录、统计图表、预算管理一站式完成，本地优先、数据完全自持
- 支付宝 / 微信账单一键导入：解析、去重、预览确认与批次撤销的完整工作流
- 完整备份 + 每日自动快照，Docker Compose 一键部署

### 新功能

- **收支记录**：类型 / 分类 / 标签 / 日期 / 金额 / 关键词多维筛选，分页与增删
  改查；筛选结果实时汇总条；桌面侧边栏与移动端底部导航双布局，全局快速
  记账入口
- **统计分析**：收支概览与环比、每日收支趋势（双轴）、分类构成环图、分类
  与标签排行、关键指标与储蓄率，支持本月 / 本季 / 本年 / 自定义时间范围
- **预算管理**：月度 / 年度预算，执行进度与超支状态分档提示
- **账单导入**：标准 JSON/CSV、支付宝 CSV（GB18030）、微信 XLSX；预览 →
  勾选 → 确认 → 批次撤销工作流；重复识别与来源元数据保留
- **完整备份**：手动快照、业务时区每日 03:00 自动快照、一键恢复（含维护
  模式与中断回滚保护）
- **登录与安全**：初始化 Token 创建账户、用户名密码登录、会话 Cookie、
  安全响应头与登录限流
- **分类与标签管理**：预设 + 自定义分类，图标与颜色可配
- **偏好设置**：界面明暗主题（跟随系统 / 浅色 / 深色）；业务时区可配置，
  影响今天 / 本月 / 本季 / 本年统计口径
- **部署**：GitHub Actions 自动构建发布镜像至 ghcr.io；健康检查跟随端口；
  Docker Compose 一键启动

### 修复

交付前经对抗性审查与稳定性加固，重点包括：

- 交易分类类型一致性校验，防止跨类型错配污染报表
- 预算月度口径按 start_date 判定生效，年度预算不参与月度加总
- 错误码治理：限流状态码不再降级、非 JSON 请求体返回 400、标签数量上限
- 登录时序侧信道防护；限流容量清理不再误删冷却中的记录
- 零元账单解析、容器数据卷只读初始化、CI 原生模块编译与依赖安装
- lockfile 补齐 Linux 平台可选依赖，修复 CI 安装失败

[1.0.0]: https://github.com/baoxinwen/ledger/releases/tag/v1.0.0
